function stl {
	if [ -h $@ ]; then
		stl "$(readlink $@)"
	else
		st "$(cygpath -w $@)"
	fi
}

YELLOW='\e[33m\]'
RESET='\e[0m\]'   

export PS1="\[$YELLOW\][\h:\w]\[$RESET\] "

alias cdc='cd /cygdrive/c'
alias stextra="stl ~/.bash_extra"
alias runx="run xwin -multiwindow -noclipboard" # Alternative: startxwin
alias cygwin-update="setup-x86_64.exe --no-desktop --no-shortcuts --no-startmenu --quiet-mode --packages git,subversion,vim,curl,wget,openssh,xinit,bash,coreutils,cygutils,cygwin,dos2unix,grep,gzip,less,mercurial,python,tar,which"